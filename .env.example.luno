# ==============================================================================
# Luno Exchange Configuration
# ==============================================================================
# This configuration uses:
# - Provider: Luno (cryptocurrency exchange - South Africa)
# - Strategy: TBD (cross-exchange arbitrage, triangular arb, etc.)
#
# Supported Pairs:
#   - XBTZAR (Bitcoin/South African Rand)
#   - ETHZAR (Ethereum/South African Rand)
#   - XRPZAR, LTCZAR, BCHZAR, etc.
#
# Note: Luno requires different strategies than Polymarket
#       (spot exchange vs prediction market)
# ==============================================================================

# Provider & Strategy Selection
PROVIDER=luno
STRATEGY=cross_exchange  # Coming soon - placeholder

# Luno API Credentials
LUNO_API_KEY_ID=your_api_key_id_here
LUNO_API_KEY_SECRET=your_api_key_secret_here
LUNO_DEFAULT_PAIR=XBTZAR  # Default trading pair

# Trading Profile (capital-based optimization)
TRADING_PROFILE=auto   # Auto-select based on balance

# Trading Parameters
ORDER_SIZE=0.01        # BTC amount per trade
ORDER_TYPE=FOK         # Fill-or-Kill recommended
TARGET_SPREAD=0.005    # 0.5% minimum spread for profit

# Risk Management
MAX_DAILY_LOSS=0       # Maximum daily loss (0 = use profile default)
MAX_POSITION_SIZE=0    # Maximum position per trade (0 = use profile default)
MAX_TRADES_PER_DAY=0   # Maximum trades per day (0 = use profile default)
MAX_BALANCE_UTILIZATION=0.5  # Use max 50% of balance per trade

# Operation Mode
DRY_RUN=true           # Start with simulation mode
VERBOSE=true           # Show detailed logs
USE_RICH_OUTPUT=true

# Statistics
ENABLE_STATS=true
TRADE_LOG_FILE=trades_luno.json

# WebSocket (for real-time market data)
USE_WSS=true  # Luno streaming API

# Advanced Settings
COOLDOWN_SECONDS=5     # Wait between trade attempts (Luno is fast)
MIN_BALANCE_REQUIRED=100.0  # Minimum ZAR 100 to operate

# Rate Limits (Luno API)
# REST: 300 calls/minute
# WebSocket: 50 concurrent sessions
