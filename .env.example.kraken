# Kraken Cross-Exchange Arbitrage Configuration
# Trade on one of the most trusted cryptocurrency exchanges
# Use with cross-exchange arbitrage strategy

# ==================== PROVIDER & STRATEGY ====================
PROVIDER=kraken
STRATEGY=cross_exchange

# ==================== KRAKEN CREDENTIALS ====================
KRAKEN_API_KEY=your_api_key_here
KRAKEN_API_SECRET=your_api_secret_base64_here  # Base64-encoded private key

# ==================== TRADING PAIRS ====================
# Kraken uses different pair naming (XXBTZUSD = BTC/USD)
PAIR=XXBTZUSD
PROVIDER_B=binance

# Common Kraken pairs:
# XXBTZUSD = BTC/USD
# XETHZUSD = ETH/USD
# XXBTZEUR = BTC/EUR
# XETHXXBT = ETH/BTC

# ==================== STRATEGY PARAMETERS ====================
MIN_SPREAD_PCT=0.4                # Minimum 0.4% spread to execute
ORDER_SIZE=0.01                   # BTC amount per trade
MAX_IMBALANCE=0.5                 # Max 50% inventory imbalance

# ==================== RISK MANAGEMENT ====================
MAX_DAILY_LOSS=100.0              # Stop after $100 daily loss
MAX_TRADES_PER_DAY=30             # Max 30 trades per day
MIN_BALANCE_REQUIRED=200.0        # Minimum $200 balance
MAX_POSITION_SIZE=1000.0          # Max $1000 per position
MAX_BALANCE_UTILIZATION=0.7       # Use max 70% of balance

# ==================== GENERAL SETTINGS ====================
DRY_RUN=true                      # Start with simulation mode
SIM_BALANCE=2000                  # Simulated starting balance
ENABLE_STATS=true                 # Enable statistics tracking
TRADE_LOG_FILE=trades_kraken.json
USE_RICH_OUTPUT=true
VERBOSE=false
SCAN_INTERVAL=10                  # Scan every 10 seconds

# ==================== IMPORTANT NOTES ====================
# Kraken API Setup:
# 1. Create API key at https://www.kraken.com/u/security/api
# 2. Enable "Query Funds" and "Create & Modify Orders" permissions
# 3. DO NOT enable "Withdraw Funds" for safety
# 4. Copy the "Private Key" (it's base64-encoded)
# 5. Set API key nonce window if needed
#
# Trading Fees:
# - Maker: 0.16-0.26% (volume-based)
# - Taker: 0.26-0.36% (volume-based)
# - Fee schedule: https://www.kraken.com/features/fee-schedule
#
# Pair Naming Convention:
# - Kraken uses X prefix for cryptocurrencies
# - Z prefix for fiat currencies
# - Example: XXBTZUSD = BTC/USD, XETHXXBT = ETH/BTC
#
# Cross-Exchange Arbitrage (Kraken + Binance):
# - Kraken offers EUR and GBP pairs (not on Binance)
# - Strong liquidity for fiat pairs
# - Trusted for large trades
# - Higher fees but better regulatory standing
#
# Fiat On-Ramps:
# - Direct USD, EUR, GBP, CAD, JPY deposits
# - Bank wire transfers supported
# - Ideal for converting crypto profits to fiat
#
# Best Practices:
# - Use Kraken for fiat exposure
# - Pair with Binance for lower-fee crypto trades
# - Monitor EUR/USD spread for triangular opportunities
# - Leverage Kraken's deep order books
