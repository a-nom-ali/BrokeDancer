# ==============================================================================
# Polymarket + Binary Arbitrage Strategy
# ==============================================================================
# This configuration uses:
# - Provider: Polymarket (prediction market)
# - Strategy: Binary Arbitrage (buy both UP/DOWN when total < $1.00)
#
# Expected Performance:
#   - Win rate: 95%+ (arbitrage is guaranteed if fills execute)
#   - Profit per trade: 0.5-2% of position size
#   - Requires: BTC 15-minute markets or similar binary outcomes
# ==============================================================================

# Provider & Strategy Selection
PROVIDER=polymarket
STRATEGY=binary_arbitrage

# Polymarket Credentials
POLYMARKET_PRIVATE_KEY=your_private_key_here
POLYMARKET_SIGNATURE_TYPE=1  # 0=EOA, 1=Magic.link, 2=Gnosis Safe
POLYMARKET_FUNDER=  # Only for Magic.link users (proxy wallet address)

# Market Configuration (auto-discovered if empty)
POLYMARKET_MARKET_SLUG=
POLYMARKET_MARKET_ID=
POLYMARKET_YES_TOKEN_ID=
POLYMARKET_NO_TOKEN_ID=

# Binary Arbitrage Strategy Parameters
TARGET_PAIR_COST=0.99  # Buy when UP + DOWN < $0.99 (1% profit margin)
ORDER_SIZE=50          # Number of shares per trade
ORDER_TYPE=FOK         # Fill-or-Kill (recommended for arbitrage)

# Trading Profile (capital-based optimization)
TRADING_PROFILE=auto   # Auto-select based on balance
# Or manually set: learning, testing, scaling, advanced, professional

# Risk Management
MAX_DAILY_LOSS=0       # Maximum daily loss (0 = use profile default)
MAX_POSITION_SIZE=0    # Maximum shares per trade (0 = use profile default)
MAX_TRADES_PER_DAY=0   # Maximum trades per day (0 = use profile default)
MAX_BALANCE_UTILIZATION=0.8  # Use max 80% of balance per trade

# Operation Mode
DRY_RUN=false          # Set to true for simulation mode
VERBOSE=true           # Show detailed logs
USE_RICH_OUTPUT=true   # Colorized console output

# Statistics
ENABLE_STATS=true
TRADE_LOG_FILE=trades_polymarket_binary_arb.json

# WebSocket (for real-time orderbook)
USE_WSS=true
POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com

# Advanced Settings
COOLDOWN_SECONDS=10    # Wait between trade attempts
BALANCE_SLACK=0.15     # Keep 15% buffer for safety
YES_BUY_THRESHOLD=0.45 # Individual side price thresholds
NO_BUY_THRESHOLD=0.45
SIM_BALANCE=1000       # Simulated balance for DRY_RUN mode

# Minimum Requirements
MIN_BALANCE_REQUIRED=10.0  # Minimum $10 USDC to operate
