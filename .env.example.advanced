# ==============================================================================
# ADVANCED PROFILE - $2,000-$5,000 Capital
# ==============================================================================
# This profile is for experienced traders with proven track record.
# Tighter spreads (1.5%) capture more opportunities with higher volume.
# Optimized for frequent trading and maximum efficiency.
#
# Expected Performance:
#   - Capital: $2,000-$5,000
#   - Spread requirement: 1.5%
#   - Position size: 50 shares per trade
#   - Max daily loss: 12% ($240-$600)
#   - Daily profit potential: $30-$50 per trade
# ==============================================================================

# Trading Profile Configuration
TRADING_PROFILE=advanced  # Options: auto, learning, testing, scaling, advanced, professional

# Polymarket API Credentials
POLYMARKET_PRIVATE_KEY=your_private_key_here
POLYMARKET_SIGNATURE_TYPE=1
POLYMARKET_FUNDER=

# Market Configuration (auto-discovered if empty)
POLYMARKET_MARKET_SLUG=
POLYMARKET_MARKET_ID=
POLYMARKET_YES_TOKEN_ID=
POLYMARKET_NO_TOKEN_ID=

# Trading Parameters (Profile will override these with optimal values)
# TARGET_PAIR_COST=0.985  # 1.5% spread (set by profile)
# ORDER_SIZE=50           # Set by profile
# COOLDOWN_SECONDS=10     # Set by profile

# Order Type
ORDER_TYPE=FOK  # Fill-or-Kill (recommended for arbitrage)

# Buy Thresholds (individual side prices)
YES_BUY_THRESHOLD=0.45
NO_BUY_THRESHOLD=0.45

# Risk Management (Profile will set these)
# MAX_DAILY_LOSS=240.0       # ~12% of $2,000 (set by profile)
# MAX_POSITION_SIZE=250      # Max 250 shares per trade (set by profile)
# MAX_TRADES_PER_DAY=60      # Max 60 trades per day (set by profile)
# MAX_BALANCE_UTILIZATION=0.40  # Use 40% per trade (set by profile)

MIN_BALANCE_REQUIRED=100.0   # Minimum $100 to operate

# Operation Mode
DRY_RUN=false    # Live trading
VERBOSE=false    # Minimal logs for performance
USE_RICH_OUTPUT=true

# Statistics
ENABLE_STATS=true
TRADE_LOG_FILE=trades.json

# WebSocket (required for this tier)
USE_WSS=true
POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com

# Balance Management
BALANCE_SLACK=0.15  # Keep 15% buffer for safety
