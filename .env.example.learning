# ==============================================================================
# LEARNING PROFILE - $100-$200 Capital
# ==============================================================================
# This profile is optimized for learning the bot with minimal capital.
# Wide spreads (3%) ensure clear profits after fees.
# DRY_RUN mode is STRONGLY RECOMMENDED for this tier.
#
# Expected Performance:
#   - Capital: $100-$200
#   - Spread requirement: 3.0%
#   - Position size: 5 shares per trade
#   - Max daily loss: 5% ($5-$10)
#   - Daily profit potential: $0.25-$0.50 per trade
# ==============================================================================

# Trading Profile Configuration
TRADING_PROFILE=learning  # Options: auto, learning, testing, scaling, advanced, professional

# Polymarket API Credentials
POLYMARKET_PRIVATE_KEY=your_private_key_here
POLYMARKET_SIGNATURE_TYPE=1
POLYMARKET_FUNDER=

# Market Configuration (auto-discovered if empty)
POLYMARKET_MARKET_SLUG=
POLYMARKET_MARKET_ID=
POLYMARKET_YES_TOKEN_ID=
POLYMARKET_NO_TOKEN_ID=

# Trading Parameters (Profile will override these with optimal values)
# TARGET_PAIR_COST=0.970  # 3.0% spread (set by profile)
# ORDER_SIZE=5            # Set by profile
# COOLDOWN_SECONDS=30     # Set by profile

# Order Type
ORDER_TYPE=FOK  # Fill-or-Kill (recommended for arbitrage)

# Buy Thresholds (individual side prices)
YES_BUY_THRESHOLD=0.45
NO_BUY_THRESHOLD=0.45

# Risk Management (Profile will set these)
# MAX_DAILY_LOSS=5.0         # $5 max loss per day (set by profile)
# MAX_POSITION_SIZE=10       # Max 10 shares per trade (set by profile)
# MAX_TRADES_PER_DAY=10      # Max 10 trades per day (set by profile)
# MAX_BALANCE_UTILIZATION=0.10  # Use only 10% per trade (set by profile)

MIN_BALANCE_REQUIRED=10.0    # Minimum $10 to operate

# Operation Mode
DRY_RUN=true     # STRONGLY RECOMMENDED for Learning profile
VERBOSE=true     # Show detailed logs for learning
USE_RICH_OUTPUT=true

# Statistics
ENABLE_STATS=true
TRADE_LOG_FILE=trades.json

# Simulation (for DRY_RUN mode)
SIM_BALANCE=150  # Simulate with $150 balance

# WebSocket (optional, for real-time price streaming)
USE_WSS=false
POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com

# Balance Management
BALANCE_SLACK=0.15  # Keep 15% buffer for safety
